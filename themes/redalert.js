/*test für ein reines css-theme welches grundlegende sachen ändert, also nicht erweitert*/
var redalert = new Theme("redalert");

redalert.themetype = "css";
redalert.description = "a theme based on red and black";
slidenote.addTheme(redalert);
